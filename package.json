{
  "name": "node-setup-with-docker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sequelize-cli db:migrate && sequelize-cli db:seed:all && node ./bin/www",
    "dev": "sequelize-cli db:migrate && sequelize-cli db:seed:all && nodemon ./bin/www",
    "debug": "DEBUG=online-exam:* npm run dev"
  },
  "dependencies": {
    "cookie-parser": "~1.4.5",
    "debug": "~4.3.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "http-errors": "~1.8.0",
    "jade": "~1.11.0",
    "morgan": "~1.10.0",
    "pg": "^8.6.0",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.6.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "sequelize-cli": "^6.2.0"
  }
}
